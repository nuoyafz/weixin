# 复制本文件为 .env，并填入你自己的真实 API Key。
# .env 已在 .gitignore 中排除，不会被提交；打包时也会被排��，密钥不进安装包。
#
# config.yaml 里对应字段写成引用形式即可：
#   text_model:
#     api_key: ${VISREPLY_API_KEY}
#
# 若环境变量中已存在同名变量，优先使用系统环境变量而非本文件。
VISREPLY_API_KEY=
